name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Download & Setup Cordova / Capacitor CLI
      run: |
        npm install -g cordova

    - name: Build Webview Package
      run: |
        mkdir -p www
        cp index.html www/
        cp IMG-20260905-WA0013.jpg www/ 2>/dev/null || true
        cp Logo.png www/ 2>/dev/null || true
        
        cordova create android_app com.alrayan.motostock "الريان قطع غيار"
        cd android_app
        rm -rf www
        cp -r ../www .
        cordova platform add android
        cordova build android --debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: AlRayan-Moto-Store-APK
        path: android_app/platforms/android/app/build/outputs/apk/debug/app-debug.apk
